package com.example.owner.myfruitapplication.exceptions;

/**
 * Our exception will be unchecked, so we only handle them in our Exception Handler ...
 */
public class MyAuthException extends RuntimeException {
}